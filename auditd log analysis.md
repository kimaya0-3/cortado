## auditd log analysis 

| RTA Name | Produces Useful Logs for Detection | Logs generated by Neo23x0 Auditd Configuration | Comments |
|---------------|---------------------------------------|-----------------------------|---------------------|
|mimipenguin_execution | Yes | <ul><li> PROCTITLE content: The decoded PROCTITLE explicitly mentions /home/kim03/.local/bin/cortado-run-rta mimipenguin_execution. Mimipenguin is a well-known credential dumping tool. Any execution of a tool like this, especially if not part of an authorized security assessment, is a critical security event. </li><li> File creation in /tmp: Creating executable-like files (ps, strings) in /tmp is a common tactic for malware to drop payloads or temporary tools. </li><li> syscall=90 (chmod) and key="perm_mod": Modifying file permissions (e.g., to make them executable) right after creation is a strong indicator of staging for execution. </li></ul>| | 
|linux_powershell_sus_child_process | Yes | <ul><li> Explicit RTA Identification and Tool Staging: The PROCTITLE for the python3 process explicitly mentions /home/kim03/.local/bin/cortado-run-rta linux_powershell_sus_child_process, directly identifying the simulation of a suspicious PowerShell child process. This process was responsible for creating nc (Netcat) and pwsh (PowerShell) executables in the /tmp directory. Both are powerful tools frequently used by attackers. </li><li> Permission Modification for Execution: Logs show syscall=90 (chmod) on /tmp/nc and an explicit chmod +x /tmp/pwsh command, both tagged with key="perm_mod". This indicates the files were made executable, a critical step before running malicious payloads. </li><li> Execution of Staged Attacker Tools: Shell processes (sh and bash) were observed executing /tmp/pwsh, tagged with key="susp_shell". Running PowerShell from a temporary directory is a significant indicator of compromise. </li><li> Attempted Execution and Crash: An ANOM_ABEND (abnormal termination) for /tmp/nc with sig=11 (segmentation fault) was recorded. Even a failed attempt to execute a staged malicious binary from /tmp is a strong indicator of compromise. </li></ul> | | 
|linux_defense_evasion_process_injection_via_dd| Yes |  <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_defense_evasion_process_injection_via_dd. This process created a file named dd in the /tmp directory, a common location for attackers to stage tools. </li><li> Permission Modification for Execution: auditd recorded syscall=90 (chmod) and an explicit chmod +x /tmp/dd command, both tagged with key="perm_mod". This indicates the staged dd binary was made executable. </li><li> Suspicious Shell Activity & Execution: Shell processes (sh, bash) were flagged with key="susp_shell", indicating their use to execute commands. This shell activity was directly linked to the execution of the staged /tmp/dd binary.</li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/dd file, tagged with key="delete", a tactic used by adversaries to remove forensic evidence. </li></ul> ||
|linux_defense_evasion_base64_xxd_decode_arg_evasion| Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_defense_evasion_base64_xxd_decode_arg_evasion. This indicates a simulated attack using xxd for base64 decoding as a defense evasion technique. This process created a file named xxd in the /tmp directory, a common location for attackers to stage tools. </li><li> Permission Modification for Execution: auditd recorded syscall=90 (chmod) and an explicit chmod +x /tmp/xxd command, both tagged with key="perm_mod". This clearly shows the staged xxd binary was made executable. </li><li> Suspicious Shell Activity & Execution: Shell processes (sh, bash) were flagged with key="susp_shell", indicating their use to execute commands. This shell activity is directly linked to the execution of the staged /tmp/xxd binary, likely to decode base64 encoded data. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/xxd file, tagged with key="delete", a common tactic used by adversaries to remove forensic evidence and hinder investigation. </li></ul> ||
|linux_execution_kill_from_executable_in_unusual_location | Yes| <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_execution_kill_from_executable_in_unusual_location. This indicates a simulated attack using the kill command from an unexpected path. This process created a file named kill in the /tmp directory, a common location for attackers to stage tools. </li><li> Permission Modification for Execution: auditd recorded syscall=90 (chmod) and an explicit chmod +x /tmp/kill command, both tagged with key="perm_mod". This clearly shows the staged kill binary was made executable. </li><li> Suspicious Shell Activity & Execution from /dev/shm: A shell process (sh) was flagged with key="susp_shell", executing /dev/shm/rta. Running executables from /dev/shm (shared memory) is highly suspicious and often used by malware for stealth or to bypass traditional file system monitoring. </li><li> Abnormal Termination of Staged kill: An ANOM_ABEND (abnormal termination) was recorded for /tmp/kill with sig=11 (segmentation fault). Even a failed attempt to execute a staged malicious binary from /tmp is a strong indicator of compromise, showing an attempt to use a kill utility from an unusual location. </li></ul>||
|linux_command_and_control_telegram_api_request| Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_command_and_control_telegram_api_request. This clearly indicates a simulated attack involving C2 communication via Telegram. This process created a file named curl in the /tmp directory, a common location for attackers to stage tools. </li><li> Permission Modification for Execution: auditd recorded syscall=90 (chmod) and an explicit chmod +x /tmp/curl command, both tagged with key="perm_mod". This clearly shows the staged curl binary was made executable, preparing it for execution. </li><li> Suspicious Shell Activity & C2 Communication Attempt: A shell process (sh) was flagged with key="susp_shell", executing the string api.telegram.org. This indicates an attempt to initiate communication with the Telegram API, likely using the staged curl binary, which is a known method for C2.</li><li> Execution of Staged Tool for C2: While the full curl command isn't in these specific logs, the combination of staging curl in /tmp, making it executable, and then a shell process attempting to interact with api.telegram.org strongly indicates the execution of the staged curl for C2 purposes.</li></ul>||
|linux_cron_hidden_execution| Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_cron_hidden_execution. This clearly indicates a simulated attack involving the use of cron for hidden execution. This process created a file named cron (likely a malicious script or executable disguised as a legitimate cron utility) in the /tmp directory, a common location for attackers to stage tools. </li><li> Permission Modification for Execution: auditd recorded syscall=90 (chmod) and an explicit chmod +x /tmp/cron command, both tagged with key="perm_mod". This shows the staged cron binary was made executable, a necessary step before it can be run. </li><li> Suspicious Shell Activity & Execution of Staged Cron: Multiple shell processes (sh, bash) were flagged with key="susp_shell". One sh process executed /tmp/cron directly, indicating the staged malicious cron was run. Another bash process also executed /tmp/cron, further confirming its execution. The process name comm="cron" for this bash execution is particularly suspicious, as it suggests an attempt to masquerade the malicious activity as a legitimate cron job. </li><li>  Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/cron file, tagged with key="delete". This is a common post-exploitation tactic to remove forensic evidence and hinder detection. </li></ul> || 
|cve_2019_14287 | Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as cve_2019_14287. This clearly indicates a simulated attack targeting this specific sudo vulnerability. This process created a file named sudo in the /tmp directory. This is highly suspicious as /tmp is not a standard location for the sudo binary, suggesting a malicious, possibly modified, version of sudo is being staged. </li><li> Suspicious Shell Activity & Execution: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning, which is often a precursor or a result of privilege escalation attempts. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/sudo file, tagged with key="delete". This is a common post-exploitation tactic to remove forensic evidence and hinder investigation, especially after attempting to exploit a vulnerability. </li></ul> ||
|gdb_init_secret_dump| Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as gdb_init_secret_dump. This clearly indicates a simulated attack involving the use of GDB to extract secrets. This process created a file named gdb in the /tmp directory. This is highly suspicious as /tmp is not a standard location for the GDB binary, suggesting a malicious, possibly modified, version of GDB or a script designed to leverage GDB is being staged. </li><li> Suspicious Shell Activity & Execution: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning. In the context of GDB secret dumping, this shell activity likely precedes or follows the GDB execution, possibly to set up the environment, execute GDB with a malicious init file, or exfiltrate the dumped secrets. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/gdb file, tagged with key="delete". This is a common post-exploitation tactic to remove forensic evidence and hinder investigation, especially after attempting to extract sensitive information. </li></ul>||
|unshadow_execution| Yes | <ul><li> Suspicious Shell Activity: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning. In the context of "unshadow execution," this shell activity would typically be used to invoke the unshadow command, often redirecting the output of /etc/passwd and /etc/shadow into a single file suitable for tools like John the Ripper or Hashcat. </li><li> Cleanup of Malicious Artifacts: The PROCTITLE of the python3 process again identifies unshadow_execution, and auditd recorded a syscall=87 (unlink/delete) targeting /tmp/unshadow, tagged with key="delete". This is a crucial indicator that the staged unshadow tool was removed after its use, a common tactic to erase forensic evidence. </li></ul>||
|multiarch_file_drops| Yes |<ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as multiarch_file_drops. This clearly indicates a simulated attack involving the staging of multiple, likely architecture-specific, malicious files. This process created a file named curl in the /tmp directory. While curl itself is a legitimate tool, its creation in /tmp and its association with a "multiarch file drops" RTA suggests it's a staged, possibly modified or malicious, version. The syscall=90 (chmod) with mode=0100775 (rwx for owner, group, and others) indicates it was made executable, preparing it for use.</li><li> Repeated Suspicious Shell Activity: A series of shell processes (sh, bash) were flagged with key="susp_shell". These shell executions (a0="sh" a1="-c" a2="--" a3="/bin/bash" and a0="/bin/bash") are repeated multiple times in quick succession. This pattern is highly indicative of an attacker attempting to execute various staged binaries or commands, potentially trying different architectures or payloads, or simply performing multiple actions related to the multiarch drop. The sheer volume and rapid succession of these shell spawns are a strong anomaly. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/curl file, tagged with key="delete". This is a common post-exploitation tactic to remove forensic evidence and hinder investigation, especially after deploying and executing multiarch tools. </li></ul>||
|eicar | Yes |<ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as eicar. This clearly indicates a simulated attack involving the Eicar test file. This process created a file named bash in the /tmp directory. While bash is a legitimate shell, its creation in /tmp and its association with the Eicar RTA suggests it's likely a staged malicious script or a renamed Eicar file designed to be executed via a shell. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for use. </li><li> Suspicious Shell Activity & Execution of Staged File: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning. This shell activity would typically be used to execute the staged /tmp/bash file, which in this RTA scenario, would contain the Eicar test string. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/bash file, tagged with key="delete". This is a common post-exploitation tactic to remove forensic evidence and hinder investigation after the Eicar test has been executed. </li></ul> |
|empire_stager| Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as empire_stager. This immediately tells us we're looking at a simulated Empire attack. This process created a file named bash in the /tmp directory. Similar to the Eicar scenario, while bash is a legitimate shell, its creation in /tmp and its association with an "empire_stager" RTA is highly suspicious. Empire stagers often involve dropping scripts (which might be named bash or something similar to blend in) that then execute further commands to pull down the full Empire agent. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for execution. </li><li> Suspicious Shell Activity & Execution of Staged File: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning. In the context of an Empire stager, this shell activity is crucial for executing the dropped script (/tmp/bash) which would then initiate the connection back to the Empire C2 server and download the next stage of the payload. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/bash file, tagged with key="delete". This is a very common tactic for stagers and other malicious tools to remove their footprint after successful execution, making forensic analysis more challenging.</li></ul> || 
|funzip_extract_content| Yes |<ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as funzip_extract_content. This clearly indicates a simulated attack involving the use of funzip (or a similar tool) to extract data from an archive. This process created two suspicious files in the /tmp directory: funzip and bash. The creation of funzip in /tmp (not a standard location for system binaries) suggests a staged, potentially modified or malicious version of the funzip utility. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for use. Similarly, the creation of bash in /tmp is suspicious. This could be a script that funzip is instructed to execute, or a temporary file used in the extraction process that an attacker might then interact with. It was also made executable (mode=0100664). Suspicious Shell Activity & Execution of Staged Files: Multiple shell processes (sh, bash) were flagged with key="susp_shell". Crucially, one sh process executes a3=2F746D702F66756E7A697020746573746E65737375735F73757465737420. The hexadecimal string 2F746D702F66756E7A697020746573746E65737375735F73757465737420 decodes to /tmp/funzip testnessus_sutest . This explicitly shows the staged funzip binary being executed from /tmp with an argument that appears to be a filename (testnessus_sutest). This is a direct indicator of the RTA's core activity. Other subsequent sh and bash executions (a3="/bin/bash") likely represent further commands to interact with the extracted content or to perform other post-extraction activities. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting both the /tmp/funzip and /tmp/bash files, tagged with key="delete". This is a common post-exploitation tactic to remove forensic evidence and hinder investigation after sensitive content has been extracted. </li></ul> ||
|darkradiation| Yes |<ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as darkradiation. This immediately tells us we're looking at a simulated DarkRadiation attack. This process created a file named xargs in the /tmp directory. While xargs is a legitimate utility, its creation in /tmp (an unusual location for system binaries) and its association with a "darkradiation" RTA is highly suspicious. DarkRadiation, like many sophisticated malware, often stages its components or uses legitimate-sounding names to blend in. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for execution. This is a crucial step for any executable malware. </li><li> Suspicious Shell Activity & Execution of Staged File: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning. In the context of a DarkRadiation simulation, this shell activity is critical. Attackers would use these shells to execute the staged xargs (or other components of the wiper), potentially passing commands to initiate its destructive payload. The repeated shell spawns could indicate attempts to execute different stages or commands of the wiper. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/xargs file, tagged with key="delete". This is a very common post-execution tactic for malware, especially wipers, to remove forensic evidence and hinder analysis after its primary objective (in this case, simulated wiping) has been achieved.</li></ul> ||
|grep_software_discovery | Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as grep_software_discovery. This immediately tells us the simulation involves using grep for reconnaissance. This process created a file named grep in the /tmp directory. While grep is a legitimate and ubiquitous Linux utility, its creation in /tmp (an unusual location for system binaries) and its association with a "grep_software_discovery" RTA is highly suspicious. Attackers often stage modified or custom versions of common tools in temporary directories to avoid detection or to use specific functionalities. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for use. </li><li> Suspicious Shell Activity & Execution of Staged File: Multiple shell processes (sh, bash) were flagged with key="susp_shell". The sh process executing a0="sh" a1="-c" a2="--" a3="/bin/bash" and the subsequent bash execution (a0="/bin/bash") indicate shell spawning. In the context of this RTA, these shells would be used to execute the staged /tmp/grep binary, likely with arguments designed to search for specific software installations or configuration files. This is a classic reconnaissance technique. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/grep file, tagged with key="delete". This is a common post-execution tactic for attackers to remove their footprint and hinder forensic analysis after gathering information. </li></ul>||
|linux_execution_kill_from_executable_in_unusual_location | Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_execution_kill_from_executable_in_unusual_location. This immediately tells us the simulation involves executing a "kill" utility from an unexpected place. This process created a file named kill in the /tmp directory. While kill is a legitimate Linux command, its creation in /tmp (which is not a standard location for system binaries) is highly suspicious. Attackers often stage their own versions of common utilities or rename malicious binaries to legitimate-sounding names to evade detection. The syscall=90 (chmod) with mode=0100775 indicates it was made executable, preparing it for use. A chmod command (comm="chmod" exe="/usr/bin/chmod") was explicitly used to change the permissions of /tmp/kill to +x (executable). This is a strong indicator of an attacker preparing a staged binary for execution.</li><li> Suspicious Shell Activity & Execution of Staged File: A sh process was flagged with key="susp_shell". Crucially, this sh process executes a0="/bin/sh" a1="-c" a2="/dev/shm/rta". This indicates a shell executing a script or binary from /dev/shm (shared memory), which is another common location for attackers to stage and execute temporary malicious files due to its writeable and executable nature. While not directly executing /tmp/kill in this specific log entry, it highlights the broader pattern of suspicious execution from non-standard locations within the RTA. Immediately following these staging and shell activities, an ANOM_ABEND (abnormal program termination) message is logged for comm="kill" exe="/tmp/kill" sig=11 res=1. This is a direct and critical indicator that the staged /tmp/kill executable was indeed executed and then terminated abnormally with signal 11 (Segmentation Fault). This is precisely what we'd expect from a simulated "kill" action or a malicious binary attempting to perform its function. </li></ul>||
|linux_execution_interactive_shell_spawn_from_hidden_process | Yes | <ul><li> Explicit RTA Identification & Hidden Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_execution_interactive_shell_spawn_from_hidden_process. This immediately tells us the simulation involves spawning an interactive shell from a hidden process. This process created a file named .evil in the /tmp directory. The leading dot (.) in the filename makes it a hidden file in Linux, a common tactic for attackers to conceal their tools. Its creation in /tmp (not a standard location for system binaries) is highly suspicious. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for use. A chmod command (comm="chmod" exe="/usr/bin/chmod") was explicitly used to change the permissions of /tmp/.evil to +x (executable). This is a strong indicator of an attacker preparing a staged binary for execution. </li><li> Suspicious Interactive Shell Spawning: A sh process was flagged with key="susp_shell". Crucially, this sh process executes a0="/bin/sh" a1="-c" a2="-i". The -i flag is used to force an interactive shell, which is a significant indicator of an attacker attempting to gain direct command-line access to the system. This interactive shell would have been spawned by the hidden /tmp/.evil executable, allowing the attacker to issue further commands. </li></ul>||
|linux_defense_evasion_hex_payload_execution| Yes | <ul><li> Explicit RTA Identification & Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_defense_evasion_hex_payload_execution. This immediately tells us the simulation involves executing an obfuscated payload. This process created a file named evil in the /tmp directory. Its creation in /tmp (not a standard location for system binaries) is highly suspicious, especially when associated with a defense evasion RTA. Attackers often drop temporary executables here. The syscall=90 (chmod) with mode=0100664 indicates it was made executable, preparing it for use. A chmod command (comm="chmod" exe="/usr/bin/chmod") was explicitly used to change the permissions of /tmp/evil to +x (executable). This is a strong indicator of an attacker preparing a staged binary for execution.</li><li> Suspicious Shell Activity with Hexadecimal Payload: A sh process was flagged with key="susp_shell". Crucially, this sh process executes a0="/bin/sh" a1="-c" a2="\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00". The a2 argument, which is typically the command or script to be executed, consists of a series of hexadecimal null bytes (\x00). This is a classic indicator of an attempt to execute a shellcode or a highly obfuscated payload directly within the shell, bypassing detection mechanisms that might look for cleartext commands or known malicious binaries. The actual payload would likely be much longer and contain executable instructions. </li></ul>||
|linux_command_and_control_ip_address_arg_from_hidden_executable | Yes | <ul><li>Explicit RTA Identification & Hidden Tool Staging: The PROCTITLE of the python3 process explicitly identifies the activity as linux_command_and_control_ip_address_arg_from_hidden_executable. This immediately tells us the simulation involves C2 communication initiated by a hidden executable. This process created a file named .evil in the /tmp directory. The leading dot (.) in the filename makes it a hidden file in Linux, a common tactic for attackers to conceal their tools. Its creation in /tmp (not a standard location for system binaries) is highly suspicious. The syscall=90 (chmod) with mode=0100775 indicates it was made executable, preparing it for use. A chmod command (comm="chmod" exe="/usr/bin/chmod") was explicitly used to change the permissions of /tmp/.evil to +x (executable). This is a strong indicator of an attacker preparing a staged binary for execution. </li><li> Command and Control Communication Attempt: A critical indicator is the execution of /tmp/.evil with specific arguments: proctitle=2F746D702F2E6576696C006E6574636F6E002D6800382E382E382E38002D70003533. Decoding this hexadecimal proctitle reveals: /tmp/.evil netcon -h 8.8.8.8 -p 53. This clearly shows the hidden executable (.evil) attempting to establish a network connection (netcon) to the IP address 8.8.8.8 on port 53. 8.8.8.8 is Google's public DNS server, often used by attackers for C2 beaconing or exfiltration attempts, as it's a commonly allowed outbound connection. Port 53 is typically used for DNS, making this a potential attempt to blend malicious traffic with legitimate DNS queries, a common C2 evasion technique. The SYSCALL msg=audit(...): arch=c000003e syscall=41 success=yes exit=3 ... comm=".evil" exe="/tmp/.evil" subj=unconfined key="network_socket_created" confirms that the hidden executable /tmp/.evil successfully created a network socket, which is a prerequisite for establishing network communication. This strongly corroborates the C2 attempt. </li><li> Cleanup of Malicious Artifacts: The RTA process was observed deleting the /tmp/.evil file, tagged with key="delete". This is a common post-execution tactic for attackers to remove their footprint and hinder forensic analysis after achieving their objective (in this case, attempting C2 communication). </li></ul> ||
